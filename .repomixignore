# Что не попадает в пак Repomix (формат — как у .gitignore).
# Этот файл — основной способ уменьшить размер пака для всех сценариев.

# --- История изменений: нужна редко, занимает много места ---
CHANGELOG.md
CHANGELOG-archive.md

# --- Разовые скрипты для наполнения БД: уже не нужны в коде ---
database/seed_callsigns.php         # Разовый PHP-скрипт для генерации

# --- Мелочь без смысловой нагрузки на код ---
LICENSE
docker/
docker-compose.yml

# --- Ниже — опциональное. Раскомментируй под конкретную задачу ---
# (или используй флаг -i в командной строке, см. гайд ниже)

# Словари интерфейса: ~15% пака.
# ВНИМАНИЕ: без них не проверить правило «ключ обязан быть и в ru, и в en».
# Выключай только когда задача не трогает пользовательские строки.
# lang/ru.php
# lang/en.php

# Английская копия README: дублирует русскую.
# README.en.md